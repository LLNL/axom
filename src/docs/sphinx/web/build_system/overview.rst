Build System Features
---------------------

TODO:
Insert blurb here about cmake with links to external pages for more detailed info.  Then mention the parts in the build - how we setup some initial paths using the host-config files, how developers can then fine tune a build enabling/disabling options, and finally building a target.

.. toctree::
   :maxdepth: 2

   configuring_for_your_platform
   fine_tuning_build_options
   supported_build_targets
