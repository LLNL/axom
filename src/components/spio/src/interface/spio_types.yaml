# spio_types.yaml
# This is generated code, do not edit
#
# Copyright (c) 2015, Lawrence Livermore National Security, LLC.
# Produced at the Lawrence Livermore National Laboratory.
#
# All rights reserved.
#
# This source code cannot be distributed without permission and
# further review from Lawrence Livermore National Laboratory.
#
# Types generated by Shroud for class SPIO
types:

  IOManager:
    base = wrapped
    forward = IOManager
    cpp_type = IOManager
    cpp_to_c = static_cast<{c_const}SPIO_iomanager *>(static_cast<{c_const}void *>({cpp_var}))
    c_type = SPIO_iomanager
    c_to_cpp = static_cast<{c_const}IOManager{c_ptr}>(static_cast<{c_const}void *>({c_var}))
    c_fortran = type(C_PTR)
    f_type = type(iomanager)
    f_derived_type = iomanager
    f_args = "{c_var}%{F_derived_member}"
    f_module:
      spio_mod:
      - iomanager
    f_return_code = "{F_result}%{F_derived_member} = {F_C_call}({F_arg_c_call_tab})"
    f_cast = "{f_var}"
    PY_format = O
    py_statements:
      intent_in:
        post_parse: |
          {cpp_var} = {py_var} ? {py_var}->{BBB} : NULL;
      intent_out:
        ctor: |
          {PyObject} * {py_var} = PyObject_New({PyObject}, &{PyTypeObject});
          {py_var}->{BBB} = {cpp_var};
    LUA_type = LUA_TUSERDATA
    LUA_pop = ({LUA_userdata_type} *)luaL_checkudata({LUA_state_var}, 1, "{LUA_metadata}")
    LUA_push = PUSH
